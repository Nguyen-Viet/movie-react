import React from 'react';
import NavBar from '../../components/Nav-bar';

const Layout = (props) => {
    return(
        <div>
            <NavBar/><br/>
            <main>
            </main>

            <label>catch</label><br/>
            <label>movie list</label><br/>
        </div>
    )
}

export default Layout;